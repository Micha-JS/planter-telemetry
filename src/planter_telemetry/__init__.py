"""IoT telemetry pipeline for the ESP32 planter sensor pod."""

# Single source of truth for the project version: hatchling reads it from here
# (see [tool.hatch.version] in pyproject.toml). Keep the plain assignment form
# so hatchling's version regex matches.
__version__ = "0.1.0"
