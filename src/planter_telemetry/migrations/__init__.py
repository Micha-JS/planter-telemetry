"""Alembic migration environment (ships inside the package wheel).

Migrations are hand-written raw SQL, sequentially numbered (0001, 0002, ...),
and never edited after merge. See docs/storage.md for the schema story and
`planter_telemetry.migrate` for the runtime entrypoint.
"""
